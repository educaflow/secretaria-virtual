# Verificación — iteración 1

{"id":"P-001","severidad":"MINOR","fichero":"design/design.md","ubicacion":"Paso 4.5 «Otras funciones» → `getCertificadoHabilitado` y tabla «Reglas de negocio R-CertificadoDigital-NNN» (fila R-CertificadoDigital-004)","origen":"design-contract.md §5 (mapeo de capas) / validacion.md §2.e","problema":"R-CertificadoDigital-004 se declara como regla de negocio `R-` pero no se ubica en la capa que la taxonomía exige: no es un método `fireActionRule_*` invocado desde `insert`/`update`/`remove` ni desde una operación custom, sino un helper privado de solo lectura (`getCertificadoHabilitado`) del bloque «Otras funciones», y su columna «Momento» dice «Lectura (no escribe; no participa de insert/update)», un valor que no existe en la taxonomía (solo Antes/Después de `repository.save/remove`).","correccion":"O bien retirar el identificador `R-CertificadoDigital-004` y documentar `getCertificadoHabilitado` simplemente como helper de lectura compartido por `getAlmacenClaveByDni`/`getSituacionFirmaByDni` (dejando la trazabilidad del comportamiento del `# Objetivo` en una nota), o bien reubicar la regla en una capa admitida por §5 con momento Antes/Después real."}
{"id":"P-002","severidad":"MINOR","fichero":"design/design.md","ubicacion":"Sección «Clasificación cliente / servidor de los campos de CertificadoDigital» (filas `nombre` y `apellidos`) + Paso 4.4 `fireActionRule_AsignarTitular`","origen":"design-contract.md §3 (coherencia obligatoria) y §9 (checklist: «ningún cliente aparece asignado por una R-Antes-de-Crear»)","problema":"`nombre` y `apellidos` se clasifican `cliente` y a la vez se asignan desde `fireActionRule_AsignarTitular` (R-CertificadoDigital-001), que es una R con momento `Antes` invocada desde `insert`. La regla de §3 prohíbe explícitamente que un campo `cliente` aparezca asignado por una R-Antes-de-Crear, porque eso lo convierte implícitamente en `servidor`. El diseño lo reconoce en «Notas y supuestos» nota 1 pero no lo resuelve dentro de la taxonomía.","correccion":"Documentar el caso en la propia tabla de clasificación como una categoría explícita (p.ej. «cliente con sobrescritura condicional del servidor») y añadir la excepción razonada en la sección de frontera de confianza, o reclasificar los campos y ajustar la R para que el diseño no incumpla literalmente el punto del checklist de §9."}
{"id":"P-003","severidad":"MINOR","fichero":"design/design.md","ubicacion":"Paso 3 — «Verificar»","origen":"design-contract.md §8 («Indicar qué verificar al final… ¿qué grep confirma que está bien?»)","problema":"El comando de verificación `grep -n 'allowPropertiesGetDatosTitularByDni' src/main/java/com/educaflow/subsystem/criptografia/` no puede funcionar: apunta a un directorio sin la opción `-r`, así que grep aborta con «Is a directory» y código 2 en vez de buscar. El detector equivalente del Paso 10 (nº 6) sí usa `grep -rn`, de modo que los dos pasos son incoherentes entre sí.","correccion":"Cambiar el comando del Paso 3 a `grep -rn \"allowPropertiesGetDatosTitularByDni\" src/main/java/com/educaflow/subsystem/criptografia/`, igual que el detector nº 6 del Paso 10."}
{"id":"P-004","severidad":"MINOR","fichero":"design/domains/CertificadoDigital.xml","ubicacion":"`<finder-method name=\"findHabilitadosByDni\" …/>` (y sus referencias en design.md Paso 2 y Paso 4.5)","origen":"domain-models.xsd, tipo `Finder`, atributo `name` («As a convention, always use `findBy` prefix»)","problema":"El finder nuevo se llama `findHabilitadosByDni`, que rompe el prefijo `findBy` que la propia definición del esquema de dominios fija como convención y que siguen los dos finders existentes del proyecto (`findByDni` en `User.xml` y `CertificadoDigital.xml`, `findByEstado` en `Correo.xml`).","correccion":"Renombrar el finder a una forma con prefijo `findBy` (p.ej. `findByDniHabilitados` o `findByDniAndEnabled`) y propagar el nombre a `design.md` (Paso 2, Paso 4.5, detector nº 1 del Paso 10) y a la nota 11 de «Notas y supuestos»."}

# Verificación — iteración 2

OK-CORRECTO

