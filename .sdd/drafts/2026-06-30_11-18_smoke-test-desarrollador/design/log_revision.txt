# Verificación — iteración 1
{"id":"P-001","severidad":"IMPORTANT","fichero":"design/design.md","ubicacion":"Paso 5 — Seguridad y tabla 'Ficheros a crear o modificar', fila `src/main/resources/data-init/input/auth.xml`","origen":"k-datainit §2 (CRITICAL) y §5 (anti-patrón)","problema":"El diseño planea modificar el auth.xml global (`src/main/resources/data-init/input/auth.xml`) para añadir la DEFINICIÓN del permiso `SmokeTest.all` (bloque `<permission name=\"SmokeTest.all\" object=\"...\">...<can .../>...</permission>`). k-datainit §2 establece como regla CRITICAL: 'La seguridad (permisos de los objetos de un sistema/subsistema) va en un fichero `input/auth-<nombresistema>.xml` dentro del `data-init` de ese sistema/subsistema, no en la carpeta global.' El §5 lo refuerza como anti-patrón: 'MUST NOT acumular en `src/main/resources/data-init/input/` los datos y permisos de todos los sistemas/subsistemas.' El diseño ya crea correctamente `subsystem/smoketest/data-init/input/auth-smoketest.xml` con esa misma definición de permiso, por lo que incluirla también en el auth.xml global es redundante y viola la convención.","correccion":"Eliminar el bloque de definición del permiso `SmokeTest.all` de la modificación del auth.xml global. La definición del permiso debe existir ÚNICAMENTE en `subsystem/smoketest/data-init/input/auth-smoketest.xml`. Si el proyecto requiere asignar el permiso al grupo `admins` mediante el auth.xml global (porque ahí se gestionan las definiciones de grupos), conservar únicamente la referencia de asignación (`<permission name=\"SmokeTest.all\"/>` dentro del bloque del grupo `admins`), no la definición completa. Verificar con k-datainit si el binding de asignación grupo-permiso también puede hacerse desde el data-init del propio subsistema."}
{"id":"P-002","severidad":"MINOR","fichero":"design/test-e2e-desc.md","ubicacion":"T-005, cabecera — campo `Verifica`","origen":"tests-e2e.md §1 (regla: 'Verifica: lista de V-/R-/U- que ejerce')","problema":"T-005 declara `Verifica: V-SmokeTest-001, U-smoke-test-001`, pero ningún paso del test verifica que los campos de fecha sean de solo lectura (que es lo que define U-smoke-test-001). T-005 solo prueba que al guardar con texto vacío el sistema muestra el mensaje 'El texto es obligatorio' y no crea el registro; no hay ningún paso que intente editar los campos de fecha ni compruebe su atributo readonly. Incluir U-smoke-test-001 en `Verifica` es incorrecto porque el test no ejerce esa regla de UI.","correccion":"Cambiar la cabecera de T-005 de `Verifica: V-SmokeTest-001, U-smoke-test-001` a `Verifica: V-SmokeTest-001`. Si se desea cubrir U-smoke-test-001 en los tests E2E, añadir un test específico (p.ej. T-006 de tipo `UI`) que intente modificar los campos `fechaCreacion` o `fechaUltimaModificacion` directamente y verifique que no son editables."}

# Verificación — iteración 2
{"id":"P-001","severidad":"MINOR","fichero":"design/design.md","ubicacion":"Paso 3 — Vistas: SmokeTest.xml → Verificar","origen":"design-contract.md §9 (checklist: sin placeholders)","problema":"El comando de verificación usa `...` como placeholder para la ruta del XSD.","correccion":"Sustituir `...` por la ruta real del XSD."}
{"id":"P-002","severidad":"MINOR","fichero":"design/test-e2e-desc.md","ubicacion":"T-001 paso 3, T-002 paso 3, T-003 paso 3, T-004 paso 3, T-005 paso 3","origen":"tests-e2e.md §3 (checklist)","problema":"Los tests referencian el botón «Añadir un nuevo smoke test» pero screen-smoke-test.md lo nombra «Nuevo».","correccion":"Alinear la referencia en los tests con el nombre «Nuevo» del screen-smoke-test.md, o actualizar la pantalla."}

# Verificación — iteración 3
OK-CORRECTO
