# Verificación — iteración 1
{"id": "P-001", "severidad": "BLOCKING", "fichero": "design/design.md", "ubicacion": "Paso 8 — Seguridad (data-init): auth-smoketest.xml (bloque <group code=admins>) e input-config.xml (<bind node=group ...>)", "origen": "k-datainit (priority descendente: grupos creados en auth.xml global priority=-1, cargado el ULTIMO); validacion.md 2.i (coherencia con el codebase); Seguridad del spec (Administrador con CRUD sobre SmokeTest)", "problema": "El diseno enlaza el permiso SmokeTest.all al grupo admins DENTRO del data-init del propio subsistema (bloque <group code=admins> en auth-smoketest.xml + <bind node=group search=self.code=:code create=false> en input-config.xml) con priority=10. Pero los grupos admins/users se crean en el auth.xml GLOBAL con priority=-1, que se carga el ULTIMO (priority descendente: 10 antes que -1). En priority=10 el grupo admins aun NO existe, asi que el group-bind con create=false no encuentra nada y SmokeTest.all jamas queda enlazado a admins. Ademas el design solo modifica el auth.xml global para QUITAR PdfUtilities.all de users, no para ANADIR SmokeTest.all a admins. Resultado: el Administrador no recibe el permiso de objeto SmokeTest.all -> acceso/lectura/CRUD denegados -> falla el requisito de seguridad central del spec. Tambien contradice la convencion de k-datainit (el auth-<sistema>.xml del subsistema define SOLO permisos; el enlace grupo->permiso vive en el auth.xml global).", "correccion": "Dejar en auth-smoketest.xml solo la definicion <permission name=SmokeTest.all ...> y eliminar el bloque <group code=admins>; eliminar el <bind node=group ...> de input-config.xml (dejar solo el bind de permission). Anadir <permission name=SmokeTest.all/> dentro del bloque <group code=admins> del fichero global src/main/resources/data-init/input/auth.xml (ya listado como Modificar en la tabla de ficheros), de modo que el enlace se cree en priority=-1, cuando el permiso ya existe."}

# Verificación — iteración 2
OK-CORRECTO

