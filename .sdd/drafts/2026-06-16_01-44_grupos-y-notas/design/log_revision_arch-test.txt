# Verificación tests arquitectura — iteración 1
{"id":"P-001","severidad":"BLOCKING","fichero":"design/arch-test-desc.md","ubicacion":"C13","origen":"com.educaflow.system.gruposnotas.service.impl.NotaMediaCalculator","problema":"El diseño (fuente de verdad: design.md lineas 15, 303-305, 644) ubica `NotaMediaCalculator` en `..service.impl..` y la entidad `AlumnoGrupo` (`..db..`) depende de el via el cuerpo CDATA de `notaMedia`. Como `..service.impl..` es subpaquete de `..service..`, el diseno TAL CUAL viola C13 (resultado real FAIL), pero la regla declara `Resultado esperado: PASS`. Ademas el texto instruye a `/sdd-implementer-system` a REUBICAR la utilidad fuera de `..service..`, es decir, modifica el diseno para que la regla pase, cuando el rol arch MUST NOT alterar el diseno (test-arquitectura.md §7)."  ,"correccion":"Describir C13 respecto al diseno tal cual: con `NotaMediaCalculator` en `..service.impl..`, la dependencia `AlumnoGrupo`→`..service.impl..` hace que C13 no se cumpla. No instruir reubicaciones ni proponer cambios de diseno. Si la situacion es una incoherencia del diseno, reportarla como tal; el fichero de tests no puede inventar una ubicacion para forzar PASS."}
{"id":"P-002","severidad":"BLOCKING","fichero":"design/arch-test-desc.md","ubicacion":"C13","origen":"com.educaflow.system.gruposnotas.util","problema":"La regla C13 cita el paquete `com.educaflow.system.gruposnotas.util..` como destino propuesto de `NotaMediaCalculator`, pero ese paquete NO existe en el inventario del diseno (design.md no crea ninguna clase en `..gruposnotas.util..`; la utilidad esta en `..service.impl..`). Paquete inventado (test-arquitectura.md §7.1 «Sin invencion»).","correccion":"Eliminar la referencia al paquete inventado `com.educaflow.system.gruposnotas.util..` (y a `base/util`). Atenerse a los paquetes que el diseno realmente define para `NotaMediaCalculator` (`..service.impl..`)."}
{"id":"P-003","severidad":"BLOCKING","fichero":"design/arch-test-desc.md","ubicacion":"C13","origen":"C13","problema":"La regla contempla pasar C13 a FREEZE «para esa unica clase» si `NotaMediaCalculator` se mantiene en `..service.impl..`. Es codigo NUEVO; un FREEZE sobre codigo nuevo es BLOCKING (test-arquitectura.md §7.1 «Resultado coherente»: FREEZE solo se justifica con una clase preexistente que el diseno modifica).","correccion":"Eliminar la opcion de FREEZE para `NotaMediaCalculator`/C13 (codigo nuevo). El resultado de C13 debe ser PASS o, si el diseno tal cual la viola, debe corregirse la incoherencia en origen, no congelarse."}
{"id":"P-004","severidad":"IMPORTANT","fichero":"design/arch-test-desc.md","ubicacion":"Cobertura","origen":"C13","problema":"La seccion «Cobertura» (linea 146, «Reglas en FREEZE: ninguna … se evita reubicando la utilidad fuera de `..service..`, ver C13») repite el razonamiento de reubicacion/invencion de C13, afirmando que la dependencia entidad→servicio «se evita» con una reubicacion que el diseno no contempla.","correccion":"Ajustar la nota de «Cobertura»/«Reglas en FREEZE» para que no dependa de una reubicacion inexistente en el diseno, en coherencia con la correccion de C13."}

# Verificación tests arquitectura — iteración 2
OK-CORRECTO

# Verificación tests arquitectura — iteración 1
{"id":"P-001","severidad":"MINOR","fichero":"design/arch-test-desc.md","ubicacion":"Regla C21 — apartado «Sujetos del diseño»","origen":"design.md §Paso 2 (campos @Inject)","problema":"Afirma que GrupoServiceImpl, AlumnoGrupoServiceImpl y NotaServiceImpl inyectan ModelServiceFactory; según design.md solo AlumnoGrupoServiceImpl lo inyecta.","correccion":"Reformular: solo AlumnoGrupoServiceImpl inyecta ModelServiceFactory; GrupoServiceImpl inyecta GrupoRepository y base abstracta de ModuloGrupo; NotaServiceImpl inyecta NotaRepository."}

# Verificación tests arquitectura — iteración 2
OK-CORRECTO
