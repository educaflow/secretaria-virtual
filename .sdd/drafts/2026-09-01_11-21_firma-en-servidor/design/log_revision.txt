# Verificación — iteración 1

{"id":"P-001","severidad":"IMPORTANT","fichero":"design/test-e2e-desc.md","ubicacion":"T-006 — «Sin certificado en el servidor se ofrece AutoFirma», sección Precondiciones/Pasos","origen":"tests-e2e.md §1 (cada test es autosuficiente e independiente; ningún test presupone más estado que «Estado inicial de la base de datos») / ESC-006","problema":"T-006 exige que NO exista entrada de certificado digital para el DNI del secretario (29050788V), pero no la borra en sus pasos, mientras que T-007 (ESC-007) crea un certificado habilitado para ese mismo DNI y no lo elimina al terminar. El resultado es un test dependiente del orden: si T-007 se ejecuta antes que T-006 (o se reejecuta la tanda sin recrear la BD), el secretario ya tendrá situación FICHERO_CON_CLAVE y T-006 verá el panel de firma en servidor en lugar del de AutoFirma, fallando. Todos los demás tests que dependen de un certificado sí empiezan borrando la entrada del DNI.","correccion":"Añadir a T-006 un primer paso equivalente al de T-001/T-002/T-003/T-007/T-011: el Administrador inicia sesión, abre la pantalla de certificados digitales y borra, si existe, la entrada del DNI «29050788V»; después cierra sesión y continúa con el login del secretario. Alternativamente, hacer que T-007 borre la entrada que crea al finalizar, pero el patrón del resto de los tests (borrar al empezar) es el que ya sigue el proyecto en src/test/e2e/subsystem/criptografia/."}
{"id":"P-002","severidad":"MINOR","fichero":"design/domains/TareaFirma.xml","ubicacion":"líneas 18-19 (<decimal name=\"x\">, <decimal name=\"y\">) y 37-39 (atributo description de los <item> de EstadoTareaFirma)","origen":"validacion.md §1 (validación mecánica con validate.sh; cada FAIL es fallo bloqueante)","problema":"validate.sh termina con «FAIL: design/domains/TareaFirma.xml» y «VALIDACION-XML: FAIL» por 5 errores: los nombres 'x' e 'y' no casan con el patrón del XSD y el atributo 'description' no está permitido en <item>. Los cinco son heredados del fichero real y el diseño los declara conservados a propósito (Notas y supuestos §1), pero tres de ellos (los 'description') son atributos que el generador ignora y podrían eliminarse sin ningún efecto funcional, con lo que el diseño reduciría la superficie de incumplimiento sin tocar columnas de BD ni código.","correccion":"Sustituir description=\"...\" por help=\"...\" en los tres <item> del enum EstadoTareaFirma (mismo criterio que ya se aplica a los seis items nuevos de SituacionFirma), dejando 'x' e 'y' intactos como exención documentada, y actualizar Notas y supuestos §1 para reflejar que solo quedan dos incumplimientos inevitables."}
{"id":"P-003","severidad":"MINOR","fichero":"design/test-e2e-desc.md","ubicacion":"T-010 — «Cada firmante solo ve sus propias tareas pendientes», sección Precondiciones","origen":"tests-e2e.md §3 (checklist: «¿Ningún test presupone más estado previo que el descrito en esa sección?»)","problema":"T-010 declara como precondición «Ninguna de las tareas de firma precargadas ha sido resuelta todavía», que es estado previo adicional al de «Estado inicial de la base de datos» y depende del orden de ejecución respecto a T-001, T-002, T-003, T-009 y T-011. En la práctica la precondición es innecesaria: las únicas tareas sobre las que T-010 asevera («Firma de prueba 5», «Firma de prueba del secretario», «Firma de prueba del administrador 1») no las resuelve ningún otro test.","correccion":"Eliminar esa precondición y dejar únicamente «El estado inicial de la base de datos descrito arriba», o bien acotarla a las tres tareas concretas sobre las que el test asevera, para que T-010 no presuponga más estado del permitido ni dependa del orden."}
{"id":"P-004","severidad":"MINOR","fichero":"design/design.md","ubicacion":"Paso 10, apartado «10.2 input-config.xml — el binding (delta)»","origen":"design-contract.md §1.3 (conservación por defecto de todo lo preexistente en una fila «Modificar»)","problema":"El diseño afirma que se añade un <input> al final del <xml-inputs> existente «(los cinco <input> actuales se conservan)», pero src/main/resources/data-demo/input-config.xml contiene diez <input>: 1 de centros-demo.xml, 4 de usuarios-demo.xml (usuarios, centroUsuarios, tipos, cargos) y 5 de permisos-demo.xml. Un implementador que tome el recuento literal como contrato de conservación podría dar por bueno un fichero al que le falten cinco <input> y romper la carga de permisos de demo.","correccion":"Corregir el recuento a «los diez <input> actuales se conservan», o mejor, redactarlo sin número: «se añade un <input> al final, conservando íntegros todos los <input> existentes del fichero»."}
{"id":"P-005","severidad":"MINOR","fichero":"design/views/Pendiente-TareaFirma.xml","ubicacion":"<field name=\"claveFirma\"> de los paneles paso2FirmarDispositivoSinPin (colSpan=\"3\") y paso2FirmarFicheroSinClave (colSpan=\"4\"); ASCII Layout correspondiente en design.md §«ASCII Layout» del Paso 7","origen":"vistas.md §1.7 y §3.g (checklist de maquetación de k-vistas/forms.md: «¿Los bordes de columna se alinean entre filas y con los paneles condicionales anidados?»)","problema":"El mismo campo claveFirma se dibuja con anchos distintos según el estado: 3 columnas en el panel del PIN y 4 en el panel de la contraseña. El texto del diseño afirma que «los dos paneles que la piden empiezan la fila en la columna 1, así que sus bordes están alineados entre sí», lo que solo es cierto del borde izquierdo: el derecho no se alinea. Además contradice el criterio que el propio diseño aplica dos párrafos más abajo a los paneles de botones, donde exige el mismo reparto 3+4+5 en todos los estados precisamente para que el elemento no cambie de tamaño ni de posición al cambiar de situación de firma.","correccion":"Unificar el colSpan del <field name=\"claveFirma\"> en los dos paneles (4 columnas, que es el ancho que admite el título más largo, «Contraseña»), actualizar los dos dibujos del ASCII Layout del Paso 7 para que ambos muestren cccc········, y corregir la frase sobre la alineación de bordes."}

# Verificación — iteración 2

{"id":"P-001","severidad":"IMPORTANT","fichero":"design/design.md","ubicacion":"Paso 10, apartado «10.3 Las dos clases Java», método TareaFirmaDemoLoader.crearDocumentos, punto 3 de la Secuencia","origen":"design-contract.md §8 (cada paso debe indicar a nivel de estructura qué se crea, con la información suficiente para implementarlo) / spec §Recursos y datos iniciales (tareas de firma precargadas con el PDF de ejemplo como documento a firmar) / ESC-001, ESC-002, ESC-003","problema":"El diseño describe la creación del documento original de cada tarea de demo como «crea un MetaFile propio con MetaFileUtil», pero com.educaflow.base.util.MetaFileUtil no tiene ningún método que produzca un MetaFile con fileName y fileType rellenos: createMetaFileInstance() devuelve un MetaFile vacío y uploadContent(metaFile, bytes) solo sube el contenido. Un MetaFile así queda con fileType == null y fileName == null, y MetaFileHelper.getDocumentoPdf(...) —que es lo que invoca la FASE 1 de fireActionRule_FirmarDocumentosEnServidor (design/rules/R-TareaFirma-001.md) sobre documentoFirma.getDocumentoOriginal()— lanza RuntimeException(\"El MetaFile no tiene fileType definido\") en MetaFileHelper.isPdf. Consecuencia: la firma en servidor de TODAS las tareas de demo falla siempre con «No se han podido firmar los documentos: …» y ESC-001/002/003 (T-001/T-002/T-003) no pueden pasar. Además, con fileName nulo el grid subsysFirmas.Pendiente@TareaFirma.DocumentoFirma-grid (columna y orderBy sobre documentoOriginal.fileName) muestra filas sin texto, con lo que los pasos 14-15 de ESC-003 quedan sin ancla.","correccion":"Reescribir el punto 3 de la secuencia de TareaFirmaDemoLoader.crearDocumentos para que el MetaFile se construya con un fileName y un fileType explícitos y válidos: leer el PDF del classpath, envolverlo en un DocumentoPdf con DocumentoPdfFactory.getDocumentoPdf(bytes, \"documento_ejemplo_firma.pdf\") y crear cada copia con MetaFileHelper.createMetaFile(documentoPdf) (que ya fija fileName y fileType = application/pdf), en vez de con MetaFileUtil.createMetaFileInstance() + uploadContent. Si se prefiere seguir usando MetaFileUtil, el diseño MUST decir explícitamente que antes de subir el contenido hay que hacer metaFile.setFileName(...) y metaFile.setFileType(MetaFileHelper.PDF_MIME_TYPE), y que cada documento necesita su propio nombre distinguible cuando numeroDocumentos es 2 («Firma de prueba 3»)."}

# Verificación — iteración 3

{"id":"P-001","severidad":"IMPORTANT","fichero":"design/design.md","ubicacion":"## Reglas del spec descartadas — segundo matiz sobre VAL-TareaFirma-005 (y su contraparte en Paso 3 y en rules/R-TareaFirma-001.md «Cuál clave se usa»)","origen":"RN-TareaFirma-006, VAL-TareaFirma-005, entity-TareaFirma.md §Campos","problema":"El diseño exige el PIN al firmante en la situación DISPOSITIVO_SIN_PIN (panel con campo «PIN», RUI-007/010 y V-TareaFirma-005 con mensaje «El PIN es obligatorio») pero después lo DESCARTA sin usarlo: el Paso 3 y rules/R-TareaFirma-001.md declaran que para tipoCertificado DISPOSITIVO_PKCS11 se construye AlmacenClaveDispositivo(slot, alias) y «claveAcceso no interviene» porque el PIN lo aporta EntornoCriptografico. Comprobado en el código real que AlmacenClaveDispositivo no admite PIN y que EntornoCriptografico.getDispositivoCriptografico(slot) lo lee de la configuración del servidor, así que el PIN tecleado nunca llega a la firma. Contradice RN-TareaFirma-006 para el caso de dispositivo, y la frase «se implementa igual […] Queda lista para cuando el PIN deje de ser obligatorio» es falsa: aunque el PIN dejara de ser obligatorio, la firma seguiría sin usar el PIN tecleado porque falta la vía en la infraestructura de criptografía. La limitación solo aparece enterrada en el Paso 3 y en el fichero de la regla, no en «Notas y supuestos» ni en la matriz.","correccion":"Corregir el matiz de «Reglas del spec descartadas» para que diga la verdad (V-TareaFirma-005 se implementa, pero la clave tecleada NO se usa en la rama de dispositivo) y añadir una entrada explícita en «Notas y supuestos» que declare la desviación acotada respecto a RN-TareaFirma-006: para DISPOSITIVO_SIN_PIN el PIN tecleado se descarta porque AlmacenClaveDispositivo(slot, alias) no tiene forma de recibirlo y el PIN lo aporta EntornoCriptografico; dejar anotado que habilitar de verdad ese caso exigirá además ampliar la infraestructura de criptografía (un AlmacenClaveDispositivo que acepte PIN), fuera del alcance de esta iniciativa."}
{"id":"P-002","severidad":"MINOR","fichero":"design/rules/R-TareaFirma-001.md","ubicacion":"## Diseño detallado → ### Diagrama de secuencia, línea campoFirma = new CampoFirma(new Rectangulo(x, y, width, height)).setNumeroPagina(page)","origen":"—","problema":"TareaFirma.x/y/width/height son <decimal> (BigDecimal en la entidad generada) y com.educaflow.base.infrastructure.pdf.Rectangulo es un record Rectangulo(float x, float y, float width, float height): la construcción tal como está escrita no compila. El código preexistente que hace lo mismo (TareaFirmaController.firmarDocumentosConAutoFirma) usa .floatValue() en los cuatro valores.","correccion":"Escribir en el diagrama de secuencia (y en el comentario de fireActionRule_FirmarDocumentosEnServidor) la conversión explícita con .floatValue() en los cuatro valores, igual que hace TareaFirmaController.firmarDocumentosConAutoFirma."}

# Verificación — iteración 4

OK-CORRECTO

