<?xml version="1.0" encoding="UTF-8"?>
<domain-models xmlns="http://axelor.com/xml/ns/domain-models"
               xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
               xsi:schemaLocation="http://axelor.com/xml/ns/domain-models https://axelor.com/xml/ns/domain-models/domain-models_8.1.xsd">

    <module name="expedientes" package="com.educaflow.subsystem.expedientes.db"/>
    <entity name="TipoExpediente">
        <string name="code"  />
        <string name="name" namecolumn="true" translatable="true" />
        <!--<enum name="ambitoCreador" ref="AmbitoTipoExpediente" title="Ámbito creador" />
        <enum name="ambitoResponsable" ref="AmbitoTipoExpediente" title="Ámbito responsable"  />
        <enum name="ambitoAuditor" ref="AmbitoTipoExpediente" title="Ámbito auditor"  />-->
        <!--
            El paquete de la carpeta de versión del tipo de expediente. De él cuelga un paquete por
            fase, así que ExpedienteLocator compone el FQCN del PhaseEventManager y del
            StateEventValidator con el codePhase del expediente; y de él cuelga también la clase
            States que el build genera. Lo reescribe el data-init en cada arranque, de modo que
            mover la carpeta del tipo se corrige solo.
        -->
        <string name="basePackageName"  title="Paquete base de las clases del tipo de expediente" />
        <many-to-one name="tramite" ref="Tramite" />

        <!--
            El puente a la máquina de estados tipada del tipo de expediente (la clase States que el
            build genera desde su TipoExpedienteInstance.xml). La entidad es un objeto por tipo
            recuperado de la base de datos, así que hace de singleton polimórfico y es el punto de
            entrada cuando el tipo concreto no se conoce en compilación.

            ExpedienteLocator es un bean inyectable, pero aquí se pide con Beans.get porque una
            entidad JPA no la construye Guice: no hay dónde inyectarlo. Es el único punto del
            subsistema donde ese service-locator es inevitable.

            El cuerpo del método va en una sola línea a propósito: es Java embebido en XML, sin
            ayuda del IDE, y se inyecta sin control de imports (de ahí los FQCN). Los cinco métodos
            de búsqueda viven una sola vez, en la clase generada, y se llega a ellos por la interfaz.

            Es <extra-code-model> y no <extra-code> porque en AOP el primero inyecta en la clase de
            la ENTIDAD y el segundo en la del Repository (Entity.toJavaClass()/toRepoClass() de
            axelor-tools). Es el mismo tag que usa el getDocumentoPdf de las entidades de expediente.
        -->
        <extra-code-model><![CDATA[

            public com.educaflow.subsystem.expedientes.services.eventmanager.TipoExpedienteStates getTipoExpedienteStates() {
                return com.axelor.inject.Beans.get(com.educaflow.subsystem.expedientes.services.internal.ExpedienteLocator.class).getTipoExpedienteStates(this);
            }

        ]]></extra-code-model>
    </entity>

    <enum name="Profile">
        <item name="CREADOR" title="Creador"/>
        <item name="RESPONSABLE" title="Responsable"/>
        <item name="SECRETARIO" title="Secretario"/>
        <item name="DIRECTOR" title="Director"/>
        <item name="AUDITOR" title="Auditor" />
    </enum>
    <!--<enum name="AmbitoTipoExpediente">
        <item name="CENTRO" title="Centro"/>
        <item name="DEPARTAMENTO" title="Departamento"/>
        <item name="CICLO" title="Ciclo"/>
        <item name="CURSO" title="Curso"/>
        <item name="GRUPO" title="Grupo" />
        <item name="MODULO" title="Módulo" />
        <item name="INDIVIDUAL" title="Individual" />
    </enum>-->

</domain-models>