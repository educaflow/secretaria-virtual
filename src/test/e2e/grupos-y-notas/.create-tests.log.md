# Log autónomo — /sdd-create-tests-e2e (grupos-y-notas)

Fecha: 2026-06-27. Modo autónomo: ante cualquier bloqueo se anota aquí y se continúa con el siguiente test, sin preguntar al usuario.

## Selección
- Iniciativa: grupos-y-notas (`.sdd/drafts/2026-06-16_01-44_grupos-y-notas`)
- Tests `[x]` en índice (17): T-001, T-002, T-003, T-004, T-005, T-011, T-012, T-013, T-016, T-017, T-018, T-019, T-020, T-021, T-024, T-025, T-026
- Saltados por `.spec.ts` ya existente (1): T-001
- A generar en esta pasada (16): T-002, T-003, T-004, T-005, T-011, T-012, T-013, T-016, T-017, T-018, T-019, T-020, T-021, T-024, T-025, T-026

## Eventos
- INFO: app caída al inicio, puerto 8080 libre. Banner de snapshot OK en .desc.md. auth.ts presente.
- INFO: app READY (200) — lanzando validación de auth.ts (§9.0)
- INFO: AUTH-OK (helper validado contra la app real; sin cambios).

## Bucle por test

### T-002 — FAIL (BLOQUEADO, posible regresión)
- Generador BLOQUEADO: al guardar grupo con nombre duplicado la app muestra el error genérico "Violación de restricción única" en lugar del mensaje de negocio esperado "Ya existe un grupo con ese nombre en este centro y curso académico".
- Diagnóstico del subagente: la restricción única de BD salta en el auto-flush de Hibernate ANTES de que `GrupoServiceImpl.validateInsert` devuelva su BusinessMessages. Regresión coherente con commit f04ab2a (flyway→data-init).
- Acción autónoma: NO se toca código Java (prohibido). No se genera .spec.ts fiel. Se marca FAIL y se continúa. **Requiere revisión humana del backend (posible regresión).**
### T-003 — SUCCESS
- Generador ESCRITO → runner GREEN (1 passed) → verificador OK. Nota: el mensaje real es 'Nombre es requerido' (validación de campo requerido de Axelor), semántica equivalente.
### T-004 — SUCCESS (1 ciclo de sanación)
- Generador ESCRITO → runner RED (no idempotente: nombres de grupo fijos + restricción única chocan al reejecutar) → sanador CORREGIDO (nombres únicos por ejecución con Date.now(), alumno libre del seed Alumno6, teardown try/finally que borra los grupos creados) → runner GREEN → verificador OK.
- INFO: añadidos timeouts a playwright.config.ts (test=90s, expect=20s, action=20s, navigation=30s; antes action/navigation eran 0=sin límite) para que el runner falle rápido. Usuario eligió mantener pipeline completo.
### T-005 — SUCCESS
- Generador ESCRITO (reutilizó t-001/t-004 como referencia) → runner GREEN → verificador OK. Idempotente (Date.now + teardown, alumnos Alumno3/Alumno4).
### T-011 — SUCCESS
- Generador ESCRITO → runner: 1er run RED transitorio, luego 3/3 GREEN estable (flake por sesión del generador aún viva) → verificador OK. Verificado que 'Reabrir grupo' solo existe en la vista de administrador, no en la del supervisor. NOTA: deja un grupo cerrado no borrable (teardown no puede); nombre único evita colisiones.
### T-012 — SUCCESS
- Generador ESCRITO → runner GREEN (24s) → verificador OK. Admin crea grupo en CIPFP Batoi eligiendo centro+curso académico. Equivalencias reales: catálogo '1º 1SMR', curso académico se renderiza '2,024'.
- INFO: detectado y eliminado worker Playwright + Chromium headless huérfanos (~44min vivos, PID 15016 y árbol) que bloqueaban la sesión de navegador del generador. App OK (200). Reanudo T-013.
### T-013 — SUCCESS
- Generador ESCRITO (test complejo cruza supervisor→admin; subagente lo dejó verde 3×) → runner GREEN (19s) → verificador OK. Idempotente con pre-clean defensivo + teardown. NOTA: subagente tardó mucho (~53min) por la complejidad; se eliminó antes un Chromium huérfano que bloqueaba.
### T-016 — SUCCESS
- Generador ESCRITO (rápido) → runner GREEN (6.8s) → verificador OK. Mensaje real 'Curso es requerido'.

### T-017 — FAIL (BLOQUEADO, regresión CONFIRMADA igual que T-002)
- Generador BLOQUEADO: al renombrar un grupo a un nombre ya existente sale "Violación de restricción única" en vez de "Ya existe un grupo con ese nombre en este centro y curso académico".
- **PATRÓN CONFIRMADO** (2 tests): T-002 (crear) y T-017 (renombrar) fallan por lo mismo → la restricción única de BD salta en el auto-flush antes de que validateInsert/validateUpdate de GrupoServiceImpl emita su BusinessMessages. Regresión coherente con commit f04ab2a (flyway→data-init).
- Acción autónoma: NO se toca Java. No se genera .spec.ts. FAIL. **Requiere revisión humana del backend: el mensaje de negocio de nombre duplicado no llega al usuario ni al crear ni al renombrar.**
### T-018 — SUCCESS
- Generador ESCRITO → runner GREEN (9.1s) → verificador OK. Mensaje 'Debe elegir un alumno', panel sin filas de datos. Idempotente.

### T-019 — FAIL (BLOQUEADO, mismo patrón de regresión)
- Generador BLOQUEADO: añadir dos veces el mismo alumno al grupo da "Violación de restricción única" al guardar, no el comportamiento de negocio V-AlumnoGrupo-001 (el alumno debe seguir apareciendo una sola vez, sin error genérico).
- **PATRÓN CONFIRMADO (3 tests): T-002, T-017, T-019** — todas las reglas de unicidad (nombre de grupo duplicado, alumno duplicado en grupo) emergen como error genérico de restricción única de BD en vez de mensaje/comportamiento de negocio. Causa común: la restricción única de BD salta en el auto-flush antes de la validación del servicio. Regresión coherente con commit f04ab2a (flyway→data-init).
- Acción autónoma: NO se toca Java. FAIL. **Requiere revisión humana del backend (es un fallo sistémico de las validaciones de unicidad).**
### T-020 — SUCCESS
- Generador ESCRITO → runner GREEN (6.9s) → verificador OK. Aserta presencia de alumnos del propio centro Y ausencia de Alumno1 CIPFP Batoi (otro centro) y del profesor. Idempotente.
### T-021 — SUCCESS
- Generador ESCRITO → runner GREEN (8.1s) → verificador OK. Crea grupo en Batoi (admin) y en Mislata (supervisor); aserta que el supervisor ve el suyo y NO el de Batoi. Idempotente, teardown borra ambos.
### T-024 — SUCCESS
- Generador ESCRITO → runner GREEN (4.4s) → verificador OK. Cierra el grupo de verdad y aserta campo Nombre deshabilitado (toBeDisabled) + valor intacto. NOTA: la UI impide editar preventivamente (campo deshabilitado), por lo que el mensaje literal 'No se puede modificar un grupo cerrado' no llega a dispararse; el verificador lo acepta porque la regla queda probada. Deja un grupo cerrado no borrable (nombre único evita colisiones).
### T-025 — SUCCESS
- Generador ESCRITO → runner GREEN (6.4s) → verificador OK. Cierra el grupo de verdad, intenta borrarlo (confirma diálogo) y aserta mensaje 'No se puede borrar un grupo cerrado' + el grupo persiste. Deja grupo cerrado no borrable (nombre único).
### T-026 — SUCCESS
- Generador ESCRITO (verde 2× consecutivas, teardown robusto con filtro de columna por la paginación) → runner GREEN (13.2s) → verificador OK. Aserta como alumno: ve su grupo y NO ve el grupo ajeno (existe de verdad, creado por supervisor con otro alumno).

## RESUMEN FINAL
- Procesados: 16 tests [x] (T-001 saltado por .spec.ts ya existente).
- SUCCESS (13): T-003, T-004, T-005, T-011, T-012, T-013, T-016, T-018, T-020, T-021, T-024, T-025, T-026
- FAIL/BLOQUEADO (3): T-002, T-017, T-019 — TODOS por la MISMA regresión del backend: las reglas de unicidad (nombre de grupo duplicado al crear/renombrar; alumno duplicado en grupo) emergen como error genérico "Violación de restricción única" en vez del mensaje/comportamiento de negocio. La restricción única de BD salta en el auto-flush de Hibernate antes de que validate*/GrupoServiceImpl emita su BusinessMessages. Coherente con commit f04ab2a (flyway→data-init). REQUIERE REVISIÓN HUMANA DEL BACKEND (no se tocó Java, modo no-regresión del skill).

## POST: suite completa verde (tick autónomo 2026-06-27)
- Al ejecutar la suite entera aparecieron 2 fallos por la BD compartida poblada (no por fidelidad):
  - T-001 (preexistente, no generado aquí): nombre fijo "1º DAM A" + sin teardown → no idempotente. SANADO: nombre único Date.now() + teardown + alumnos Alumno3/4.
  - T-026: su teardown por filtro de grid fallaba en silencio → dejaba Alumno1/Alumno2 pegados a grupos residuales (V-AlumnoGrupo-005) y bloqueaba runs posteriores. Desbloqueado (borrados 4 grupos t026 huérfanos por SQL) y SANADO: pre-limpieza defensiva self-healing + teardown fiable. 3× verde.
- RESULTADO FINAL: `npx playwright test src/test/e2e/grupos-y-notas --workers=1` → **14 passed**.
- NOTA SISTÉMICA: la BD es compartida y NO se resetea; se acumulan ~grupos cerrados no borrables (T-011/T-024/T-025) y residuos. Los tests son idempotentes/defensivos, pero para correr la suite entera de forma fiable conviene resetear la BD entre pasadas completas, o todos los tests que fijan alumnos necesitan pre-limpieza defensiva. Pendiente de decisión del usuario (no se hizo wipe masivo ni reset autónomo).
- RECORDATORIO: T-002/T-017/T-019 siguen FAIL por la regresión de backend (mensaje de unicidad) — requiere revisión humana del código Java.
